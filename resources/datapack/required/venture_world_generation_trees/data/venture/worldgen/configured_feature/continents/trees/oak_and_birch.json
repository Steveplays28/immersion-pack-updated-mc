{
  "type": "bigglobe:script",
  "config": {
    "script": [
      "if (random.nextDouble(0.0, 1.0) > 0.5:",
        "return(placeFeature(originX, originY, originZ, 'venture:continents/trees/oak'))",
      ") else (",
        "return(placeFeature(originX, originY, originZ, 'venture:continents/trees/birch'))",
      ")"
    ]
  }
}
